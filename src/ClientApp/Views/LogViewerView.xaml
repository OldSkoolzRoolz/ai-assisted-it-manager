<UserControl x:Class="KC.ITCompanion.ClientApp.Views.LogViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="4">
            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <TextBox Width="180" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
            <TextBlock Text="Level:" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <ComboBox Width="120" SelectedItem="{Binding SelectedLevel}" ItemsSource="{Binding Levels}" Margin="0,0,8,0"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}"/>
        </DockPanel>
        <ListView Grid.Row="1" ItemsSource="{Binding Entries}" VirtualizingPanel.IsVirtualizing="True" FontFamily="Consolas" FontSize="12">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Time" Width="170" DisplayMemberBinding="{Binding Timestamp}"/>
                    <GridViewColumn Header="Level" Width="70" DisplayMemberBinding="{Binding Level}"/>
                    <GridViewColumn Header="Category" Width="200" DisplayMemberBinding="{Binding Category}"/>
                    <GridViewColumn Header="EventId" Width="60" DisplayMemberBinding="{Binding EventId}"/>
                    <GridViewColumn Header="Message" Width="600" DisplayMemberBinding="{Binding Message}"/>
                    <GridViewColumn Header="Session" Width="140" DisplayMemberBinding="{Binding Session}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>