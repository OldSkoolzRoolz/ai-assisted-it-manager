<UserControl x:Class="ClientApp.Views.PolicySettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:ClientApp.ViewModels"
             mc:Ignorable="d">
    <UserControl.Resources>
        <!-- Explicit key-based template (omit DataType to avoid namespace resolution issues) -->
        <DataTemplate x:Key="PolicySettingTemplate">
            <Border BorderBrush="#DDD" BorderThickness="1" Margin="4" Padding="8" CornerRadius="4">
                <StackPanel>
                    <!-- Header row: Enabled + Label (if any) -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,6">
                        <CheckBox IsChecked="{Binding Enabled}" Content="Enabled" Margin="0,0,8,0" />
                        <TextBlock Text="{Binding PartId}" FontWeight="Bold" />
                    </StackPanel>

                    <!-- Editors switch by ValueType -->
                    <ContentControl Content="{Binding}" Margin="0,0,0,4">
                        <ContentControl.Style>
                            <Style TargetType="ContentControl">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock Text="(No editor)" Foreground="Gray" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <!-- Boolean: just checkbox already present; hide editor -->
                                    <DataTrigger Binding="{Binding ValueType}" Value="Boolean">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                    <!-- Numeric -->
                                    <DataTrigger Binding="{Binding ValueType}" Value="Numeric">
                                        <Setter Property="ContentTemplate">
                                            <Setter.Value>
                                                <DataTemplate>
                                                    <TextBox Width="140" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" ToolTip="Numeric value" />
                                                </DataTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                    <!-- Text -->
                                    <DataTrigger Binding="{Binding ValueType}" Value="Text">
                                        <Setter Property="ContentTemplate">
                                            <Setter.Value>
                                                <DataTemplate>
                                                    <TextBox Width="220" TextWrapping="Wrap" AcceptsReturn="True" Height="60" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />
                                                </DataTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                    <!-- Enum -->
                                    <DataTrigger Binding="{Binding ValueType}" Value="Enum">
                                        <Setter Property="ContentTemplate">
                                            <Setter.Value>
                                                <DataTemplate>
                                                    <ComboBox Width="220"
                                                              ItemsSource="{Binding EnumItems}"
                                                              SelectedValuePath="Value" DisplayMemberPath="Name"
                                                              SelectedValue="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />
                                                </DataTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentControl.Style>
                    </ContentControl>

                    <!-- Errors -->
                    <ItemsControl ItemsSource="{Binding Errors}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Foreground="Red" Text="{Binding}" FontSize="11" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <!-- Responsive wrapping panel for policy parts -->
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding SelectedPolicySettings}" ItemTemplate="{StaticResource PolicySettingTemplate}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel ItemWidth="300" MinWidth="300" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </ScrollViewer>
</UserControl>