<UserControl
    x:Class="ITCompanionClient.Controls.StatusView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid RowSpacing="16" Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Spacing="12">
            <StackPanel Width="180" Spacing="4">
                <TextBlock Text="Overall Health" FontWeight="SemiBold" />
                <Border Background="{StaticResource Brush.Surface.Mid}" Padding="8" CornerRadius="6">
                    <TextBlock Text="OK" FontSize="24" FontWeight="Bold" />
                </Border>
            </StackPanel>
            <StackPanel Width="180" Spacing="4">
                <TextBlock Text="Active Alerts" FontWeight="SemiBold" />
                <Border Background="{StaticResource Brush.Surface.Mid}" Padding="8" CornerRadius="6">
                    <TextBlock Text="0" FontSize="24" FontWeight="Bold" />
                </Border>
            </StackPanel>
            <StackPanel Width="180" Spacing="4">
                <TextBlock Text="Policy Drift" FontWeight="SemiBold" />
                <Border Background="{StaticResource Brush.Surface.Mid}" Padding="8" CornerRadius="6">
                    <TextBlock Text="0" FontSize="24" FontWeight="Bold" />
                </Border>
            </StackPanel>
        </StackPanel>
        <Grid Grid.Row="1" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Expander Header="Recent High Importance Events" IsExpanded="True" Grid.Column="0">
                <ListView />
            </Expander>
            <Expander Header="Active Alerts" IsExpanded="True" Grid.Column="1">
                <ListView />
            </Expander>
        </Grid>
    </Grid>
</UserControl>