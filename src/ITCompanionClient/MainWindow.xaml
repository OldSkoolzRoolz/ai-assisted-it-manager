<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ITCompanionClient.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ITCompanionClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="IT Companion (WinUI)">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.Resources>
            <local:LocalizedStrings x:Key="Loc" />
        </Grid.Resources>
        <controls:NavigationView x:Name="RootNav" PaneDisplayMode="Left" IsBackButtonVisible="Collapsed" IsSettingsVisible="True" SelectionChanged="OnNavSelectionChanged">
            <controls:NavigationView.MenuItems>
                <controls:NavigationViewItem Content="{Binding Provider[Nav_Status], Source={StaticResource Loc}}" Icon="Home" Tag="status" />
                <controls:NavigationViewItem Content="{Binding Provider[Nav_Policies], Source={StaticResource Loc}}" Icon="Library" Tag="policies" />
                <controls:NavigationViewItem Content="{Binding Provider[Nav_Logs], Source={StaticResource Loc}}" Icon="Document" Tag="logs" />
                <controls:NavigationViewItem Content="{Binding Provider[Nav_Watchers], Source={StaticResource Loc}}" Icon="Contact" Tag="watchers" />
                <controls:NavigationViewItem Content="{Binding Provider[Nav_DevDiag], Source={StaticResource Loc}}" Icon="Edit" Tag="devdiag" />
            </controls:NavigationView.MenuItems>
            <controls:NavigationView.PaneFooter>
                <StackPanel Orientation="Horizontal" Spacing="8" Padding="8" VerticalAlignment="Bottom">
                    <TextBlock Text="{Binding Provider[UI_ChangeLanguage], Source={StaticResource Loc}}" Margin="0,0,4,0" />
                    <ComboBox x:Name="CultureCombo" Width="140" SelectionChanged="OnCultureSelectionChanged" />
                    <Button Content="{Binding Provider[UI_ApplyLanguage], Source={StaticResource Loc}}" Click="OnApplyLanguage" />
                </StackPanel>
            </controls:NavigationView.PaneFooter>
            <Frame x:Name="ContentFrame" />
        </controls:NavigationView>
    </Grid>
</Window>
